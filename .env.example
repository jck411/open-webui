# Open WebUI Configuration
# Copy this file to .env and modify as needed

# Vector Database (Use Milvus to avoid ChromaDB SQLite FTS5 issues)
VECTOR_DB=milvus

# Future: Switch to PostgreSQL with pgvector
# VECTOR_DB=pgvector
# PGVECTOR_DB_URL=postgresql://user:pass@localhost:5432/openwebui
